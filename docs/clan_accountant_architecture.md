# Clan Accountant Bot — Описание архитектуры
# (Русское название: «Казначей клана»)

> **Политика этого файла:** здесь фиксируются только **отлаженные и принятые в продакшен** изменения.
> Экспериментальные версии, отладочные ветки и черновики сюда не вносятся.

---

## **Текущая версия:** 0.2.5 (от 2026-05-16)

История версий: [CHANGELOG.md](CHANGELOG.md)

**Точка входа:** `clan_monitor/clan_accountant.py`  
**Запуск:** `run_accountant.bat` (в корне `accountant_bot/`) или вручную `python clan_accountant.py [--force]`

---

## Назначение и управление

Проект представляет собой многоцелевую аналитическую систему для игры Mech Heroes, решающую три задачи:
1.  **Мониторинг клана («Казначей клана»):** Отслеживание прогресса и активности членов клана ОРДА.
2.  **Глобальная аналитика Арены:** Слежение за Топ-50 игроками мира, эволюцией их отрядов и рейтингов.
3.  **Аналитика боёв и личный прогресс:** Детальный разбор сражений и ведение досье на оппонентов для игрока `ksotar`.

### Безопасность сессии и флаг `--force`
Для предотвращения разрыва текущей игровой сессии (ошибка `61081: Can't find context`) в системе встроена защита. Скрипты проверяют наличие активного соединения с сервером игры. 
*   **По умолчанию:** Если вы играете, бот пропустит сетевые запросы, чтобы не «выбить» вас из игры.
*   **Использование `--force`:** Этот флаг принудительно игнорирует проверку сессии. Его стоит использовать, если игра была закрыта некорректно или если нужно обновить отчет, несмотря на активную сессию.

---

## Публикация и доступ

Система использует гибридную схему публикации результатов:
*   **GitHub Pages (Git):** Недельные отчеты по активности клана и основной дашборд «Казначея».
*   **Внешний хостинг (FTP):** Интерактивный дашборд Арены, отчеты по отрядам и личная статистика боев. Публикуется по адресу: `http://ovalhalla.ru/my/mech/arena.html`.

---

## Оркестрация и расписание (Windows)

Для автономной работы системы по расписанию используется файл **`run_accountant.bat`** в корне проекта. Он выполняет роль «дирижёра» всей системы:

1.  **Логирование:** Все действия записываются в `logs/accountant.log`.
2.  **Изоляция среды:** Использование `C:\tools\Anaconda3\python.exe`.
3.  **Полный цикл:**
    *   **Клановый этап:** `clan_monitor/clan_accountant.py` (сбор данных клана).
    *   **Аренный этап:** `arena_update.py` (сбор Топ-50, анализ боев, генерация личной статистики).
    *   **Деплой:** 
        *   `deploy.py` — выгрузка аналитики Арены и личных данных на **FTP**.
        *   `git commit/push` — публикация клановых отчетов на **GitHub Pages**.

---

## Модульная структура проекта

Проект спроектирован как модульная система, состоящая из независимых, но интегрированных подсистем:

1.  **Мониторинг клана:** Основная логика по сбору очков и генерации недельных отчетов.
    *   *Подробнее:* [Clan Monitor / Style Guide](../clan_monitor/STYLE_GUIDE.md)
2.  **Аналитика Арены:** Глобальный мониторинг Топ-50, отслеживание отрядов и сравнение билдов.
    *   *Подробнее:* [Arena Analytics Architecture](arena_analytics_architecture.md) и [Arena Documentation](arena_analytics_documentation.md)
3.  **Аналитика Боёв:** Детальный разбор сражений, история личных встреч и статистика игрока.
    *   *Подробнее:* [Battle Analytics](battle_analytics.md)

---

## Архитектура: поток данных

```
[Запуск скрипта]
       │
       ▼
  fetch_data()
  ├─ POST /init          → sessionID, иерархия клана, рейтинг клана
  └─ POST /directcommand → GetUsersRawInfos → данные профилей всех членов клана
       │
       ▼
  generate_web_report()
  ├─ Обновляет members_name_db.json (ники, роли, аватары)
  ├─ Сохраняет снэпшот очков → snapshots/points_utc_YYYY-MM-DD_HH-MM.json
  ├─ Читает ВСЕ снэпшоты → строит понедельные периоды
  ├─ Считает прирост за каждый день для каждого игрока
  ├─ Определяет факт присутствия / ухода из клана
  ├─ Генерирует HTML-файлы отчётов → clan/ORDA/reports/report_YYYY_WNN.html
  └─ Генерирует clan/ORDA/index.html (редирект на последний отчёт)
       │
       ▼
  run_git_push()  (если AUTO_PUSH = True)
  └─ git add -A → git commit → git push
```

---

## Файлы и директории

### Входные / конфигурационные

| Файл | Описание |
|---|---|
| `config.json` | `USER_ID`, `AUTH_KEY`, `VERSION` игры. Обновляется ботом автоматически при смене версии. |
| `manual_adjustments.json` | Ручные корректировки очков. Структура: `{"YYYY-MM-DD": {"userId": [очки_при_выходе]}}`. Нужны для игроков, покинувших клан в течение дня — сервер при следующем запросе уже не вернёт их очки. |
| `members_name_db.json` | Накопительная база ников, ролей и аватаров всех участников. Ключ — `userId` (строка). Пишется при каждом запуске. |
| `translations_cache.json` | Кэш переводов черт аватара (борода, очки и т.д.) — чтобы не переводить повторно. |

### Снэпшоты

**Директория:** `snapshots/`

Каждый запуск создаёт файл `points_utc_YYYY-MM-DD_HH-MM.json`:
```json
{
  "pts": {"227408": 10206, "123456": 8500, ...},
  "clanRating": 11199931
}
```
Снэпшоты **никогда не удаляются** — это сырая история. По ним строятся все отчёты.

### Выходные HTML

**Директория:** `clan/ORDA/`

| Файл | Описание |
|---|---|
| `index.html` | Редирект на последний недельный отчёт |
| `reports/report_YYYY_WNN.html` | Отчёт за конкретную ISO-неделю |

---

## Ключевые модули и функции

### `fetch_data() → (hier, users, rating)`

Два сетевых запроса к API:

1. **`POST /init`** — возвращает иерархию клана (`hier`) и рейтинг клана.
   - Содержит `clanData.clanState.hierarchy` — лидер + слоты с userId и points.
2. **`POST /directcommand`** (тип `GetUsersRawInfos`) — по списку userId возвращает профили:
   - nick, avatarConfiguration и т.д.

**Auto-healing версии:** если `/init` вернул error code `61058` («outdated version»),
бот автоматически парсит новую версию из текста ошибки, обновляет `config.json` и повторяет запрос.

### `generate_web_report(hier, users, current_rating)`

Основная логика отчёта. Алгоритм по шагам:

1. **Обновление базы имён** — для каждого `user` из ответа API сохраняет `nick`, `role`, переведённые черты аватара.
2. **Сохранение снэпшота** — `pts` (userId → points) + `clanRating`.
3. **Загрузка всех снэпшотов** — сортирует по времени, группирует по неделям (ключ `YYYY_WNN`)

### 4.4. Защита активной сессии (Session Safety)
Для предотвращения разрыва текущей игровой сессии пользователя (ошибка `61081: Can't find context`) бот выполняет двухэтапную проверку перед любым обращением к API:
1.  **Проверка заголовка окна:** Поиск процесса с заголовком, содержащим "Боевые роботы".
2.  **Проверка сетевых соединений:** Если окно найдено, выполняется анализ TCP-соединений (`netstat`). Если обнаружено соединение в статусе `ESTABLISHED` с IP сервера (`tanks.ya.patternmasters.ru`), бот прекращает работу.

**Переопределение:** Использование флага `--force` при запуске (`python clan_accountant.py --force`) полностью отключает эту проверку, позволяя обновить отчёт даже при запущенной игре.

4. **Расчёт прироста (Алгоритм Абсолютного Паритета):**
   - **Сбор потока:** Бот загружает ВСЕ снэпшоты (`sd`) за неделю и фильтрует их по времени (Monday 00:00 UTC → Monday 00:00 UTC следующей недели).
   - **Накопление (Accumulation Loop):** Используется «Золотой Алгоритм» из `gen_utc_graph.py`, который корректно обрабатывает выходы/входы игроков и ручные правки:
     - `total_acc` растет только при увеличении очков или при подтвержденном «сбросе» (выход из клана с ручной фиксацией очков).
   - **Интерполяция (Interpolation):** Для каждого дня (00:00 UTC) вычисляется значение `v_i` методом линейной интерполяции по временному потоку.
   - **Суточный прирост:** `Growth = int(boundary_vals[i+1]) - int(boundary_vals[i])`. 
     - *Важно:* Использование `int()` на границах критично для совпадения с визуальными метками графиков.
   - **Manual Adjustments:** Учитываются внутри цикла накопления как моментальные инкременты `total_acc`.

5. **Определение присутствия:**
   - Сегодня: игрок присутствует, если он в текущей иерархии ИЛИ есть manual_adjustment.
   - Прошлые дни: бот проверяет наличие активности (прироста) или записей в группировках по дням.
6. **Клановая статистика:** суммарный прирост клана по дням (сумма индивидуальных приростов) + изменение рейтинга + «сожжённые» очки (прирост членов − изменение рейтинга).
7. **Генерация HTML:** один файл на неделю, навигация через `<nav>`.

---

## Стандарты точности данных (Data Parity)

Для этого проекта установлен стандарт **Bit-Perfect Parity** между табличными отчетами и графиками.

1. **Единый источник истины (SSOT):** И `clan_accountant.py`, и `gen_utc_graph.py` должны использовать идентичный блок кода накопления очков.
2. **Временная зона:** Все расчеты ведутся строго в **UTC**. Конвертация в MSK происходит только на этапе отображения времени обновления.
3. **Границы дней:** Строго 00:00:00 UTC. Никаких смещений на "последний снэпшот". Если данных на полночь нет — используется линейная интерполяция между ближайшими точками.
4. **Округление:** Всегда используется **отбрасывание дробной части** (`int()`) для значений в узлах интерполяции. 

### `translate_traits_batch(traits_list)`

Переводит черты аватара (прическа, борода, очки и т.д.) с английского на русский.
Использует жёстко зашитый словарь (без обращения к внешним API). Результат кэшируется в `translations_cache.json`.
Ранее использовался `GoogleTranslator` (библиотека `deep_translator`) — сейчас не нужен, но `import` остался.

### `run_git_push()`

Простой `subprocess`-вызов `git add -A && git commit && git push`.
Управляется флагом `AUTO_PUSH = True` в начале файла.

---

## Визуальный стиль дашборда

Зафиксирован в `STYLE_GUIDE.md` (статус: **LOCKED**, последнее изменение 28.04.2026).

- **Тема:** тёмная (`#0d1117` фон — GitHub-стиль).
- **Шрифты:** Orbitron (заголовок), Inter (текст), Roboto Mono (цифры).
- **Акцентные цвета:** синий `#58a6ff`, золото `#f2cc60`, зелёный `#3fb950`, красный `#f85149`.
- Таблица: строки кросс-недельной статистики клана + строки участников (сортировка по total очкам).

---

## API: параметры запросов

**Заголовки (все запросы):**
```
Content-Type: application/json
Origin: https://app-476209.games.s3.yandex.net
Referer: https://app-476209.games.s3.yandex.net/
User-Agent: Mozilla/5.0 ... Chrome/124.0.0.0
```

**Base URL:** `https://tanks.ya.patternmasters.ru/{VERSION}`

| Запрос | Endpoint | Ключевые поля |
|---|---|---|
| Инициализация | `POST /init?userid={USER_ID}` | `userID`, `authKey`, `locale: "ru"`, `platform`, `version` |
| Данные участников | `POST /directcommand?userid={USER_ID}` | `userId`, `sessionID`, `type: "GetUsersRawInfos"`, `request: JSON({users: [ids]})` |

---

## История версий

См. отдельный файл **[CHANGELOG.md](CHANGELOG.md)**.

---

## Известные ограничения и технический долг

- `import deep_translator` — импорт не нужен, перевод сейчас полностью на словаре. Можно убрать.
- Снэпшоты не очищаются автоматически — директория будет расти бесконечно.
- Нет обработки ситуации, когда `members_name_db.json` содержит userId игрока, которого нет в текущем ответе `GetUsersRawInfos` (был в клане давно).
- `manual_adjustments.json` нужно заполнять вручную перед запуском, если кто-то ушёл из клана.
